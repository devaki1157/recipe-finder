import React from "react";
import "./styles.css";

const SearchBar = () => {
  return (
    <input type="text" placeholder="Search recipes..." className="search-bar" />
  );
};

export default SearchBar;
